# ── Secrets & environments ────────────────────────────────────────────────────
deploy/.env
backend/.env
frontend/.env

# ── Dev-only compose override (has local secrets / port bindings) ─────────────
# Commit this only if your team shares the same dev setup
# deploy/docker-compose.override.yml

# ── Python ────────────────────────────────────────────────────────────────────
backend/venv
__pycache__/
*.pyc
*.pyo
backend/erp_club.db
backend/nohup.out
backend/conf_emails.json

# ── DB init SQL (generated, keep template only) ───────────────────────────────
deploy/init-db/erp.sql

# ── Node ──────────────────────────────────────────────────────────────────────
node_modules/
frontend/nohup.out

# ── Build artefacts ───────────────────────────────────────────────────────────
frontend/dist/
deploy/config/conf_emails.json
deploy/erp_vps.dump
deploy/erp_vps.sql

.ds-sync/
ds-bundle/
.design-sync/
